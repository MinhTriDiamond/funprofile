

# Chỉnh sửa kích cỡ và màu sắc tiêu đề + footer

## Thay đổi trong `src/components/feed/ClaimHistoryModal.tsx`

### 1. Tiêu đề (dòng 232)
- Tăng kích cỡ từ `text-xl` → `text-2xl` (tăng ~4px)
- Đổi gradient sang xanh lá kim loại đậm hơn: `from-[#1B5E20] via-[#2E7D32] to-[#1B5E20]` + thêm `drop-shadow-md` cho sang trọng

### 2. Footer tổng (dòng 319-326)
- Tăng kích cỡ từ `text-base` → `text-lg` (tăng ~1-2px)
- Đổi màu tổng CAMLY từ `text-[#FFD700]` (vàng) → `text-[#1B5E20]` (xanh lá đậm kim loại)

